# Server-side Gemini API key (Required for AI features)
GEMINI_API_KEY=

# Production Database Connection (Cloud SQL / PostgreSQL)
# When not provided, the server automatically defaults to SQLite / In-Memory storage
DATABASE_URL=
POSTGRES_URL=
